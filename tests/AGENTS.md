# AGENTS: tests/

- Purpose: Test utilities and stubs used across unit tests.
- Mocks: Place HTTP/WS stubs under `tests/mocks/`; keep them realistic and minimal.
- Unit tests: Write Vitest specs next to source files as `__tests__/*.spec.ts[x]`.
- Coverage: Target 80%+ line coverage; add regression tests for bugfixes.

