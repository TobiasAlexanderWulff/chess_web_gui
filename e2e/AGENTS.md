# AGENTS: e2e/

- Runner: Playwright. Run with `npx playwright test`.
- Scope: Human↔Engine flows; cover critical match scenarios end-to-end.
- Tracing: Enable `--trace on` to capture screenshots and traces on failure.
- Stability: Prefer deterministic setups and seeded data.

