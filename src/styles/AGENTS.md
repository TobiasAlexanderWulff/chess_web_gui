# AGENTS: src/styles/

- Purpose: Global styles, resets, variables, and shared utility classes.
- Files: Keep `reset.css` minimal; define global tokens (colors, spacing) in `global.css`.
- Scope: Avoid component-specific rules here; colocate those with components when needed.
- Conventions: Prefer CSS variables; use consistent naming and avoid !important.
- Size: Keep styles lean; remove unused rules.

